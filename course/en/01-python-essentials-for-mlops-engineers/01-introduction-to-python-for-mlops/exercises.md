# Exercises

Create a small Python script that reads a JSON or YAML configuration file and prints one parameter from it.

Then move part of the script logic into a separate function.

After that, split the code into two files: one file for the main entry point and one for helper logic.

Finally, create a virtual environment and run the script inside it.