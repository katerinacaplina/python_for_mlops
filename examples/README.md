# Examples

This folder contains runnable code examples used throughout the course.

At the moment, it includes a minimal command-line utility that supports the preparation module. More examples will be added as the course grows.